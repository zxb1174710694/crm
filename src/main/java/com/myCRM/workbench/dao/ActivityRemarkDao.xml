<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myCRM.workbench.dao.ActivityRemarkDao">

    <insert id="insertRemark" >
        insert into tbl_activity_remark(
            id,
            noteContent,
            createTime,
            createBy,
            editFlag,
            activityId

        )values (
            #{id},
            #{noteContent},
            #{createTime},
            #{createBy},
            #{editFlag},
            #{activityId}
        )
    </insert>

    <insert id="savaRemark">
       insert into tbl_activity_remark(
            id,
            noteContent,
            createTime,
            createBy,
            editFlag,
            activityId

        )values (
            #{id},
            #{noteContent},
            #{createTime},
            #{createBy},
            #{editFlag},
            #{activityId}
        )
    </insert>

    <delete id="deleteRemark">
        delete from tbl_activity_remark
        where id = #{id}
    </delete>

    <select id="getList" resultType="ActivityRemark">
        select * from tbl_activity_remark
        where activityId = #{id}
    </select>

    <select id="shouldTotal" resultType="int">
        select count(*) from tbl_activity_remark
        where activityId in
        <foreach collection="array" item="ids" open="(" close=")" separator=",">
            #{ids}
        </foreach>
    </select>

    <delete id="getTotal">
        delete from tbl_activity_remark
        where activityId in
        <foreach collection="array" item="ids" open="(" close=")" separator=",">
            #{ids}
        </foreach>
    </delete>
</mapper>